import React from 'react';
import FontAwesome from 'react-native-vector-icons/FontAwesome';

const IconEye = ({ size = 20, color = '#333' }) => (
  <FontAwesome name={'eye'} size={size} color={color} />
)

export default IconEye;
