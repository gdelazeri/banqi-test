import React from 'react';
import FontAwesome from 'react-native-vector-icons/FontAwesome';

const IconMoney = ({ size = 25, color }) => (
  <FontAwesome name={'usd'} size={size} color={color} />
)

export default IconMoney;
