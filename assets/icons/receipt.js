import React from 'react';
import FontAwesome from 'react-native-vector-icons/FontAwesome';

const IconReceipt = ({ size = 30, color }) => (
  <FontAwesome name={'file-text-o'} size={size} color={color} />
)

export default IconReceipt;
