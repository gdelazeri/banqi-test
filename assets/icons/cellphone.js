import React from 'react';
import FontAwesome from 'react-native-vector-icons/FontAwesome';

const IconCellphone = ({ size = 30, color }) => (
  <FontAwesome name={'mobile'} size={size} color={color} />
)

export default IconCellphone;
