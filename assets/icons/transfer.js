import React from 'react';
import FontAwesome from 'react-native-vector-icons/FontAwesome';

const IconTransfer = ({ size = 30, color }) => (
  <FontAwesome name={'exchange'} size={size} color={color} />
)

export default IconTransfer;
